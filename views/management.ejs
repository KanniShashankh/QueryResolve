<!DOCTYPE html>
<html>
<head>
    <title>Lab Management System</title>
    <style>
        body {
            background-color: lightskyblue;
            font-family: Arial, sans-serif;
        }

        .container {
            max-width: 1500px; /* Increased max-width value for wider container */
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        h1 {
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 8px;
            text-align: left;
            word-break: break-all;
        }

        th {
            background-color: #f2f2f2;
            font-weight: bold;
            white-space: nowrap; /* Added property to prevent text wrapping */
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        a {
            text-decoration: none;
            color: #4caf50;
        }

        a:hover {
            text-decoration: underline;
        }

        .logout-btn {
            position: absolute;
            top: 10px;
            right: 10px;
        }

        .logout-btn a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            background-color: #ccc;
            color: #fff;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Lab Management System</h1>
        <div class="logout-btn">
            <a href="/" title="Logout">
                <span class="gate-symbol">&#128682;</span>
            </a>
        </div>
        
        <h2><center>List of unresolved queries</center></h2>
        <table>
            <tr>
                <th>Query ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Roll Number</th>
                <th>Course</th>
                <th>Year</th>
                <th>Branch</th>
                <th>Section</th>
                <th>Lab</th>
                <th>Lab Room Number</th>
                <th>Query</th>
                <th>Action</th>
            </tr>
            <% for (let i = 0; i < queries.length; i++) { %>
                <tr>
                    <td><%- queries[i].id %></td>
                    <td><%- queries[i].name %></td>
                    <td><%- queries[i].email %></td>
                    <td><%- queries[i].roll %></td>
                    <td><%- queries[i].course %></td>
                    <td><%- queries[i].year %></td>
                    <td><%- queries[i].branch %></td>
                    <td><%- queries[i].section %></td>
                    <td><%- queries[i].lab %></td>
                    <td><%- queries[i].room %></td>
                    <td><%- queries[i].query %></td>
                    <td><a href="/resolve_query?id=<%- queries[i].id %>">Resolve</a></td>
                </tr>
            <% } %>
        </table>
    </div>
</body>
</html>
